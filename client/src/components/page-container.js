import styled from 'react-emotion';

export default styled('div')({
  display: 'flex',
  justifyContent: 'space-between',
  alignItems: 'center',
  maxWidth: 720,
  margin: '0 auto',
  padding: '16px 8px',
});
